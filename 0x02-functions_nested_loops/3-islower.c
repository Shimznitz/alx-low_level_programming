#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
   * _islower - prints to lowercase
i   * Return: Alway int
   */
int _islower(char c)
{
	if (islower(c) > 0)
		return (1);
	else
		return (0);

}
