#include <stdlib.h>
#include <stdio.h>
/**
 * main - prints reverse alphabets
 * Return: Always 0
 */
int main(void)
{
	 int ch = 'z';

	 while (ch >= 'a')
		 putchar(ch);
	 	 --ch;
 putchar('\n');

 return (0);
}
