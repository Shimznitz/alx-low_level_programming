#include <string.h>
#include <stdio.h>
/**
 * rev_string - reverse string
 * @c: input char
 * Return: void
 */
void rev_string(char *s)
{
	int count = strlen(s);
	char x[];
	int i;



	for (i = count; i >= 0; --i)
	{
		if (s[i] != '\n')
		{
		 x[i] = s
		}
	}
}
