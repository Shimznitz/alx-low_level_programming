#include "main.h"
#include <string.h>
/**
 * _strlen - len of string
 * @s: string in
 * Return: lenght of string
 */
int _strlen(char *s)
{
	int str_len;

	str_len = strlen(s);

	return (str_len);
}
